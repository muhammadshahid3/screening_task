version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
     - ./ngnix/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
  